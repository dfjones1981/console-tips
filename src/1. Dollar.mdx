---
name: 1. Dollar
route: /Dollar
order: 2
---

import { Playground } from "docz";
import Button from "./Button";

# $$$$

## $n variable

The $0 variable will always refer to the last element you clicked on

Use it to quickly perform queries on the currently selected element

$1...$n refer to previously selected elements.

## $_ variable

The $_ variable refers to the last evaluated expression and returns it's current value

<Playground>
  <Button>First Button</Button>
  <Button>Second Button</Button>
</Playground>
